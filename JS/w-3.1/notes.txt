--> Mongo DB 
1) Databases can't be access from browser directly. Beacuse Databases are  granual or granola access (mean completely yes or no) can be access by one person such as admin or any user they can access completly.
2) There is exception Database such as (Firebase) it provide's granual or granola access to user they can directly read and write.